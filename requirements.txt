# ============================================================================
# Python Dependencies for rmhogcapi
# ============================================================================
# Azure Functions OGC API Service (Features, STAC, Raster, xarray)
# Last Updated: 19 DEC 2025
# ============================================================================

# Azure Functions Runtime
azure-functions>=1.18.0

# PostgreSQL Database Access
psycopg[binary]>=3.1.0

# Data Validation & Configuration
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Azure Managed Identity Support
azure-identity>=1.15.0

# Logging and Monitoring
azure-monitor-opentelemetry>=1.2.0

# ============================================================================
# Raster API & xarray API Dependencies (Added 19 DEC 2025)
# For Reader App Migration - see READER_MIGRATION_PLAN.md
# ============================================================================

# HTTP Client for STAC/TiTiler service clients
httpx>=0.25.0

# xarray/Zarr Support (for xarray_api module)
xarray>=2024.1.0
zarr>=2.16.0
fsspec>=2024.2.0
adlfs>=2024.2.0       # Azure Data Lake filesystem for Zarr blob access
h5netcdf>=1.3.0       # NetCDF4 backend for xarray

# NumPy (required by xarray)
numpy>=1.24.0

# Image Processing (for xarray_api output rendering)
Pillow>=10.0.0        # PNG rendering with colormaps
rasterio>=1.3.0       # GeoTIFF creation with georeferencing
